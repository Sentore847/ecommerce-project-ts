## 🚀 Встановлення та запуск

### 1. Клонування репозиторію
```bash
git clone git@github.com:Sentore847/ecommerce-project-ts.git
cd ecommerce-generics
```

### 2. Встановлення залежностей
```bash
npm install
```

### 3. Варіанти запуску

#### Запуск в режимі розробки (швидкий)
```bash
npm run dev
```

#### Компіляція та запуск
```bash
npm run build
npm start
```

#### Компіляція з відслідковуванням змін
```bash
npm run watch
```

#### Очищення скомпільованих файлів
```bash
npm run clean
```

## 💻 Використання

### Приклад роботи з товарами
```typescript
import { Electronics } from './types/products';
import { findProduct, filterByPrice } from './utils/productUtils';

// Створення масиву товарів
const electronics: Electronics[] = [
  {
    id: 1,
    name: 'iPhone 15 Pro',
    price: 35000,
    category: 'electronics',
    brand: 'Apple',
    warranty: 12,
    inStock: true
  }
];

// Пошук товару
const phone = findProduct(electronics, 1);

// Фільтрація за ціною
const affordable = filterByPrice(electronics, 30000);
```

### Приклад роботи з кошиком
```typescript
import { addToCart, calculateTotal } from './utils/cartUtils';

// Створення кошика
let cart = [];

// Додавання товару
cart = addToCart(cart, phone, 1);

// Підрахунок суми
const total = calculateTotal(cart);
```

## 🔧 Технології

- **TypeScript 5.3** - мова програмування
- **Node.js** - середовище виконання
- **ts-node** - виконання TypeScript без компіляції

## 📚 Особливості реалізації

### Generic типи
Всі функції використовують generic типи для забезпечення типобезпечності:
```typescript
const findProduct = <T extends BaseProduct>(
  products: T[],
  id: number
): T | undefined => {
  // ...
};
```

### Типи товарів
Використовується intersection types для розширення базового типу:
```typescript
type Electronics = BaseProduct & {
  category: 'electronics';
  brand: string;
  warranty: number;
};
```

### Валідація даних
Всі функції містять перевірку коректності вхідних параметрів:
```typescript
if (!Array.isArray(products)) {
  console.error('Помилка: products має бути масивом');
  return undefined;
}
```

## 📝 Вимоги до завдання

- ✅ Всі функції типобезпечні
- ✅ Використовуються лише функції (не класи)
- ✅ Додані коментарі до функцій
- ✅ Перевірка коректності вхідних даних
- ✅ Демонстрація роботи з різними типами товарів

## 🎓 Навчальні цілі

1. ✅ Вивчення основ generic типів в TypeScript
2. ✅ Створення типобезпечних функцій
3. ✅ Застосування generics на практиці

---

**Примітка:** Цей проект створено в навчальних цілях для демонстрації роботи з generic типами в TypeScript.
